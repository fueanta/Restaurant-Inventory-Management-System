CREATE table "PURCHASE_INGREDIENT" (
    "PURCHASE_ID"   NUMBER(6,0),
    "INGREDIENT_ID" NUMBER(3,0),
    "AMOUNT"        NUMBER(10,4) NOT NULL
)
/

alter table "PURCHASE_INGREDIENT" add constraint  "PURCHASE_INGREDIENT_PK" primary key ("PURCHASE_ID","INGREDIENT_ID")
/

ALTER TABLE "PURCHASE_INGREDIENT" ADD CONSTRAINT "PURCHASE_INGREDIENT_FK" 
FOREIGN KEY ("PURCHASE_ID")
REFERENCES "POS_PURCHASE" ("ID")
ON DELETE CASCADE

/
